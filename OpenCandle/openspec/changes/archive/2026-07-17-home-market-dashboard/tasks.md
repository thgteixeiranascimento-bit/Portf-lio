# Tasks

Follow AGENTS.md (TDD mandatory, CHANGELOG, `graphify update .`). All work is GUI (`gui/web/`, `gui/server/`) plus one new GUI-server endpoint — no changes to `src/providers/`, `src/routing/`, `src/prompts/`, or `src/pi/`. Widgets are read-only; every CTA reuses the existing composer handlers (`setDraft` / submit) — never call a chat-run API from a widget. Do NOT bind `visibleDashboard.activeAnalyses` or `visibleDashboard.recentResearch` anywhere on home (they are structurally empty on `/`; see design.md D6).

## 1. Indices endpoint (server, TDD — do this first)

- [x] 1.1 Live verification spike for `^`-prefixed symbols (design R1): with real network, fetch quotes + sparklines for `^GSPC`, `^IXIC`, `^DJI`, `BTC-USD` through the same provider helpers the quote snapshot uses (`fetchQuoteSnapshot` at `gui/server/market-state-api.ts:535`, `fetchSparklineSnapshot` at `:425`). Record in the PR description: whether each symbol returns a price, what `currency` is (expect possibly `null`), what `marketState` reads, and whether sparkline points come back. If a symbol is unusable, note it — the strip's graceful-hide path covers it; do not substitute a different symbol without flagging it.
- [x] 1.2 Failing unit tests for `GET /api/market-state/indices` with `globalThis.fetch` mocked to fixture JSON (no live calls in unit tests; add fixtures under `tests/fixtures/` per repo convention): response matches the spec's documented shape — `generatedAt` plus an `indices` array of exactly four entries in the order ^GSPC/^IXIC/^DJI/BTC-USD with status/price/change/changePercent/sparkline; a query parameter cannot alter the symbol set; per-symbol provider failure yields that entry `status: "unavailable"` with a `reason` while the other three return `status: "ok"`; `currency: null` passes through without crashing.
- [x] 1.3 Failing unit test for the guard: a request that fails `allowTrustedGuiRequest` is rejected identically to the existing private market-state APIs, using the same "Market-state API" guard label as `gui/server/http-routes.ts:214-222` (copy the assertion pattern from the existing market-state API guard tests).
- [x] 1.4 Failing unit test for the SWR store: two requests inside the freshness window trigger one provider fetch; a request after expiry serves the stale snapshot immediately and refreshes in the background (mirror the `QuoteSnapshotStore` semantics in `gui/server/quote-snapshot-store.ts` and its tests).
- [x] 1.5 Implement the endpoint: fixed symbol set constant, dedicated SWR store, `allowTrustedGuiRequest` guard, per-symbol unavailability, follower-safe pure read. Put the snapshot logic beside `gui/server/market-state-api.ts` and register the route in `gui/server/http-routes.ts` alongside `/api/market-state` (`:214`) and `/api/market-state/quotes` (`:220`). Make 1.2-1.4 green.

## 2. View-model derivations (frontend pure functions, TDD)

- [x] 2.1 Failing unit tests for the shared portfolio day-move function: given lots with numeric `marketValue` and `changePercent`, returns the sum over qualifying lots of `marketValue - marketValue / (1 + changePercent / 100)`, matching the existing `ValueHeader` math at `gui/web/src/features/market-state/PortfolioPage.jsx:547-559`; lots with missing/non-finite `changePercent` or `marketValue` are excluded from the sum (not treated as 0%); all-lots-unusable returns an "unavailable" sentinel (`null`), never 0.
- [x] 2.2 Extract that math from `PortfolioPage.jsx`'s ValueHeader into a pure function in the portfolio view-model module (`gui/web/src/features/market-state/portfolio-view-model.js` or a sibling), make `PortfolioPage.jsx` consume it, and make 2.1 green. The portfolio page's displayed value must not change (assert via its existing tests or a snapshot).
- [x] 2.3 Failing unit tests for movers ordering: sorting `watchlistQuotes` descending by `abs(changePercent)` (mixed signs order correctly; missing changePercent sorts last). Implement as a pure helper and make green.
- [x] 2.4 Failing unit test for an indices client store/hook that polls `GET /api/market-state/indices` and exposes `{ loading, quotes, unavailable }`; endpoint error or zero available symbols yields the "hide the strip" state. Implement and make green.

## 3. Widget components (static-markup render tests, per the `stock-quote-card-render.test.ts` pattern)

New widget components live under a new `gui/web/src/features/home/` directory.

- [x] 3.1 Failing render tests, then implement `IndicesStrip`: renders the available fixed symbols as compact tiles (symbol, last price, signed % change colored via `quoteChangeDirections` from `gui/web/src/features/market-state/format.js`, sparkline via `gui/web/src/components/market-sparkline.jsx` — no axes, no legend); `currency: null` renders without a broken label; unavailable state renders nothing (component returns null — graceful hide, no error banner).
- [x] 3.2 Failing render tests, then implement `WatchlistMovers`: rows (symbol → sparkline → price → delta chip) from `watchlistQuotes` sorted per 2.3, capped at 5 visible rows with a "View all" link to `/watchlists` when more exist; skeleton rows (`gui/web/src/components/ui/skeleton.jsx`) when the user has watchlist symbols but no snapshot yet; empty state when no watchlist symbols; stale snapshot reuses the existing stale-quote disclosure semantics (do not invent new freshness copy).
- [x] 3.3 Failing render tests, then implement `PortfolioSummaryStrip` as a stat-tile row (not a chart): total value as the hero number in `tabular-nums`, today's move and all-time P&L as delta chips (arrow/sign + signed value), reusing the `MoneyTile`/`DeltaChip` patterns (`gui/web/src/features/renderers/cards/_shared.jsx:46`/`:82`) or the `ValueHeader` idiom — no new tile system. Totals from `portfolioSummaries` plus the day move from the shared 2.2 function; day move renders "unavailable" (not $0.00) when the sentinel comes back; empty state when no portfolios.
- [x] 3.4 Failing render tests, then implement `AlertsCard`: shapes `alerts`/`alertEvents`/`notifications` from the `/api/market-state` snapshot through the existing `alert-view-model.js`; conveys alert state with icon + text (never color alone); links to the alerts page; empty state points at alert creation. No mutation affordances.
- [x] 3.5 Failing render tests, then implement the affordance cards: "Add a watchlist" → `/watchlists`, "Track a portfolio" → `/portfolios`, shown when the corresponding saved state is empty; same `Panel` style, muted illustration-free copy, one clear action per card.
- [x] 3.6 Row-click behavior (conditional task — check, don't assume): first check whether the `symbol-page` change's `/symbol/$ticker` route exists in `gui/web/src/router.jsx`. If it does NOT exist: failing test that clicking a movers/indices row calls the composer prefill handler with `$SYMBOL ` (trailing space, `askAboutSymbol` pattern at `ChatPanel.jsx:145-158`) and focuses the composer, then implement. If it DOES exist: failing test that row click navigates to `/symbol/$TICKER`, then implement. Either way, assert no chat run starts from a row click.
- [x] 3.7 Design-system pass across all widgets: `Panel` from `gui/web/src/features/market-state/shared.jsx` and `gui/web/src/components/ui/` primitives (`Button`, `Skeleton`) only — no new primitives; `tabular-nums` on every dynamic numeral; interactive rows/CTAs get minimum 40x40px hit areas, `active:scale-[0.96]`, and scoped transition properties (never `transition: all`); any price-change flash reuses `quoteFlashClass`/`useQuoteFlashDirections` (`shared.jsx:286-292`) with its `motion-reduce` suppression; no enter/stagger animations on initial widget load; freshness surfaces only via the existing "Quotes Nm old" / "As of <date>" conventions.
- [x] 3.8 Accessibility pass with render-test assertions: each widget container is `aria-labelledby` its heading; movers/indices rows are real links or buttons with `focus-visible` rings; sparklines keep their existing `aria-label` (`market-sparkline.jsx:35`); delta chips pair color with an arrow or sign.

## 4. Dashboard composition in ChatPanel

- [x] 4.1 Failing render tests for the composed dashboard replacing/extending the `EmptyThread` branch in `gui/web/src/features/chat/ChatPanel.jsx` (the `isEmptyThread` branch at lines 247-248): heading (`text-balance`) + composer render above the widget grid; the grid is two columns at `lg`+ with the indices strip spanning full width and a single column below `lg`; suggestion chips reuse the already-computed home-prompts value from `ChatPanel.jsx:249` (`homePromptsForMarketState()` with saved state, `DEFAULT_PROMPTS` without); populated user sees indices + movers + portfolio strip + alerts card; new user (no saved state) sees indices + default chips + both affordance cards. Implement, wiring widget data from the `useMarketState()` values ChatPanel already has plus the 2.4 indices hook.
- [x] 4.2 Failing test for transcript takeover: when `visibleRows`/`activity` make `isEmptyThread` false, the dashboard is absent and the transcript branch renders exactly as before (reuse an existing transcript test as the baseline; no dashboard elements in the output).
- [x] 4.3 Failing test for model-setup precedence: when `needsSetup`, `ModelSetupCard` renders first exactly as today (the branch at `ChatPanel.jsx:305-316`) and submit stays blocked with the existing toast (`ChatPanel.jsx:195`) — including after a widget prefill updates the draft. If you choose to render read-only widgets below the setup card, they must not push it out of first position; the simplest compliant implementation keeps today's setup-card-only branch.
- [x] 4.4 Regression test for the run flow (spec "Composer stays primary"): home submit still goes through `App.jsx` `startRoutedChatRun` with `chatRunSessionTarget({pathname: "/", canStartFreshHomeSession: role === "writer"})`, one retry on `sessionChanged`, session adoption on `run.started`. Assert against the existing App-level tests for this flow (extend, don't fork) — the dashboard must introduce zero new call sites into `startChatRun`.
- [x] 4.5 Skeleton-first loading: failing test that before the first `/api/market-state/quotes` response, saved-state widgets show skeletons sized to the final widget heights (no flash of empty states, no enter animation), matching the existing market-state pages' first-load skeleton behavior. Implement.
- [x] 4.6 Mobile layout: single-column stacked widgets below the composer at small breakpoints, reusing `MobileHeader` (`gui/web/src/features/layout/AppShellChrome.jsx:15`); no document-level horizontal overflow (assert per the existing mobile layout test pattern if present, else verify via the 5.3 screenshots).
- [x] 4.7 Follower correctness: render test that the dashboard renders identically from the same read APIs when `role !== "writer"` with no writer-only affordances added by widgets.
- [x] 4.8 Projector-trap guard: add a test asserting the home dashboard components never read `activeAnalyses` or `recentResearch` from the dashboard state — render the composed dashboard with a `visibleDashboard` Proxy that records property access and assert neither `activeAnalyses` nor `recentResearch` is accessed.

## 5. Verification

- [x] 5.1 `npm test`, `npx tsc --noEmit`, and `npx biome ci .` green.
- [x] 5.2 React Doctor clean (no error-level diagnostics) on all changed GUI files.
- [x] 5.3 GUI screenshots at 1440x960 and 390x844 for the PR: (a) populated dashboard (seeded watchlist + portfolio + alert), (b) new-user empty state, (c) `needsSetup` state showing ModelSetupCard precedence.
  - Evidence: `scratch/autoreview-round2-dashboard-{1440x960,390x844}.png`, `scratch/autoreview-round2-dashboard-new-user-{1440x960,390x844}.png`, and `scratch/autoreview-round2-dashboard-needs-setup-{1440x960,390x844}.png`; headless Chromium measured `scrollWidth` 390px at a 390px viewport in every mobile state.
- [x] 5.4 Live smoke: run `npm run gui`, open `/`, confirm the indices strip shows real values (or gracefully hides, per the 1.1 spike findings), submit a prompt from the dashboard, and confirm navigation to `/sessions/$id` with a normal transcript.
- [x] 5.5 CHANGELOG `[Unreleased]` Added entry for the home market dashboard (widgets + indices endpoint), per the changelog-automation skill.
- [x] 5.6 `graphify update .`; then `npx openspec validate home-market-dashboard --strict` (verify the command exists first; if it does not, run `npx openspec status --change home-market-dashboard` and confirm all artifacts are done).
