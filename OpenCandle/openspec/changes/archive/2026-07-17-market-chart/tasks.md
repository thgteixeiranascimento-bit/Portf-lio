# Tasks

Follow AGENTS.md (TDD mandatory — failing test first; `.js` extensions on relative imports; fixtures under `tests/fixtures/`; no live API calls in unit tests). The spec (`specs/market-chart/spec.md`) is the contract; `design.md` D1–D9 and its UI/UX guidance section explain the decisions. Server work first, then component. Do not implement anything the parallel `symbol-page` change also mentions — this change owns the endpoint; `symbol-page` only consumes it.

## 1. Provider timestamp fix (TDD)

- [x] 1.1 Failing unit test in `tests/unit/providers/yahoo-finance.test.ts` (or the existing `getHistory` test file): with a mocked `globalThis.fetch` returning an intraday chart fixture (add `tests/fixtures/yahoo-finance/chart-1d-5m.json` with ≥3 distinct epoch-second `timestamp` values), assert each returned bar has `timestamp` equal to the raw fixture epoch seconds AND `date` equal to the same date-only string as before (both fields present, distinct timestamps, identical dates).
- [x] 1.2 Implement: add `timestamp?: number` to `OHLCV` in `src/types/market.ts` (JSDoc: "UTC epoch seconds; present when the provider supplies per-bar timestamps"); in `src/providers/yahoo-finance.ts` `getHistory` bar mapper (~line 510) add `timestamp: ts` alongside the existing `date` field. Change nothing else in the mapper or filter.
- [x] 1.3 Run the existing history/sparkline/backtest/correlation unit tests untouched and confirm green (proves the addition is non-breaking).

## 2. Range map + snapshot builder (TDD)

- [x] 2.1 Export `HISTORY_RANGES` and `HISTORY_INTERVALS` from `src/tools/market/stock-history.ts` (currently module-private at lines 12–25) so the GUI server imports the same literals instead of retyping them.
- [x] 2.2 Failing unit tests in `tests/unit/gui-server/instrument-history-snapshot.test.ts` for a new `resolveHistoryRange(rangeLabel, intervalOverride?)` helper: every default mapping from design D4 (1D→1d/5m, 5D→5d/15m, 1M→1mo/1h, 6M→6mo/1d, YTD→ytd/1d, 1Y→1y/1d, 5Y→5y/1wk, MAX→max/1mo); range labels matched case-sensitively (`"1d"` is unknown); out-of-depth override `("1Y", "5m")` returns `{ status: "invalid_request", reason }` with the reason naming the combination; unknown range `"7W"` and unknown interval `"2h"` return the same shape. Depth caps: 1m→7d, 5m/15m→60d, 1h→730d.
- [x] 2.3 Failing unit tests for `getInstrumentHistorySnapshot(symbol, rangeLabel, intervalOverride?)` in `gui/server/market-state-api.ts`, mocking the provider layer (mock `getHistory` via `vi.mock` of `src/providers/yahoo-finance.js`, same approach as existing `market-state-api` tests): success returns `{ symbol, range, interval, source: "Yahoo Finance", fetchedAt, dataAsOf, stale, prevClose, bars }` with `bars[].time` copied from `OHLCV.timestamp`; provider failure returns `{ status: "unavailable", reason }` without throwing; bars missing a finite `timestamp` on an intraday range (stale pre-deploy cache, design D2) yield unavailable, while a daily+ range falls back to midnight-UTC timestamps derived from `date`; `prevClose` is `null` when not derivable.
- [x] 2.4 Implement `resolveHistoryRange` and `getInstrumentHistorySnapshot()` in `gui/server/market-state-api.ts`, following the `fetchSparklineSnapshot` pattern at `gui/server/market-state-api.ts:425` (`wrapProvider("yahoo", () => getHistory(...))`, `result.status === "unavailable"` handling, `stale` passthrough, `dataAsOf` from the last bar's `date`).

## 3. SWR memo (TDD)

- [x] 3.1 Failing unit tests in `tests/unit/gui-server/history-snapshot-store.test.ts` for a `HistorySnapshotStore` mirroring `gui/server/quote-snapshot-store.ts` but keyed `symbol|range|interval`: fresh entry (<60s, injectable `now()`) served with zero builder calls; five concurrent `get()`s on a cold key invoke the builder exactly once and all resolve to its value; expired entry rebuilds; builder rejection clears the in-flight promise so the next call retries.
- [x] 3.2 Implement `gui/server/history-snapshot-store.ts` (copy the `QuoteSnapshotStore` shape: constructor takes `build`, `maxAgeMs = 60_000`, injectable `now`; single in-flight promise per key — note the store must be per-key where `QuoteSnapshotStore` is singleton, so hold a `Map<string, entry>`), and route `getInstrumentHistorySnapshot` calls through one module-level store instance.

## 4. HTTP route (TDD)

- [x] 4.1 Failing route tests in the existing gui-server http-routes test file (pattern: existing `/api/instruments/quote` tests): untrusted request → rejected by `allowTrustedGuiRequest` with no snapshot call; trusted `GET /api/instruments/history?symbol=AAPL&range=1D` → 200 JSON matching the snapshot builder output; `compare=MSFT` present → ignored (identical response); invalid range/interval → HTTP 400 with the `{ status: "invalid_request", reason }` body from `resolveHistoryRange`.
- [x] 4.2 Implement the route in `gui/server/http-routes.ts` directly below the `/api/instruments/quote` block (line 254), guard string `"Market-state API"`, method GET only, calling `getInstrumentHistorySnapshot(url.searchParams.get("symbol") ?? "", url.searchParams.get("range") ?? "1D", url.searchParams.get("interval") ?? undefined)`; write `invalid_request` results with `writeJson(res, body, 400)`, everything else with `writeJson(res, body)`. No session, no writer lock (spec: follower-safe).

## 5. MarketChart component (TDD)

- [x] 5.1 Add `"lightweight-charts": "^5"` to `gui/web/package.json` dependencies (NOT the root package.json); `npm install`; verify license is Apache-2.0 in the installed package.
- [x] 5.2 Series palette extraction (TDD): failing test asserting `gui/web/src/lib/series-colors.js` exports `SERIES_COLORS` with exactly the six oklch strings currently at `gui/web/src/features/market-state/PortfolioPage.jsx:26-33`, in the same order; then create the module, make `PortfolioPage` import it (delete the local `ALLOCATION_COLORS` literals; its existing modulo usage at line 566 is unchanged), and confirm the existing PortfolioPage render tests stay green.
- [x] 5.3 Failing static-render tests in `tests/unit/gui-web/market-chart-render.test.ts` using the `renderToStaticMarkup(React.createElement(...))` pattern from `tests/unit/gui-web/stock-quote-card-render.test.ts` (lightweight-charts draws on canvas post-mount, so static tests assert the component shell): container renders with the given `className`/height and an `aria-label` containing the symbol, range label, latest close, and signed change (spec: assistive-technology label); `onRangeChange` present → all eight buttons `1D 5D 1M 6M YTD 1Y 5Y MAX` in markup, the `range`-matching button has `aria-pressed="true"`, every button carries ≥40×40px hit-area classes (`min-h-10 min-w-10` or padding equivalent) and `active:scale-[0.96]`, and no `transition-all` class appears; `onRangeChange` absent → no range buttons; 2 indexed series → legend markup with one colored-dot entry per symbol whose text color classes are `text-foreground`/`text-muted-foreground` (not inline series color); 1 series → no legend markup; markup contains no `<iframe>` and no `src` containing `tradingview.com` (the no-embed guard, sibling to `tests/unit/gui-web/market-state-page-render.test.ts:144`'s `ticker-line.com` assertion).
- [x] 5.4 Failing behavior tests (jsdom + `vi.mock("lightweight-charts")` exposing spies for `createChart`, series `setData`, `applyOptions`, `chart.remove`): mount with `mode="area"` creates an area series with `{ time, value: close }` points; `mode="candlestick"` creates a candlestick series with OHLC; `mode="indexed"` with 2 series and no `indexed` prop calls `setData` with values `close/bars[0].close*100` (both starting at 100); indexed series colors are `SERIES_COLORS[0..n]` by prop position and are NOT `--tw-success`/`--tw-destructive`; re-rendering with the middle of three series removed leaves the surviving symbols' colors unchanged (color keyed by symbol, never recomputed by position); direct line-end labels render for 2–4 indexed series and not for 5–6; `prevClose` set in a single-series mode creates a horizontal price line at that value; `showVolume` creates a histogram series on a dedicated volume price-scale id with bottom-confining `scaleMargins` and no second visible price axis; grid/axis colors passed to `createChart` resolve from `--tw-border`/`--tw-muted-foreground`; crosshair tooltip content for a hovered candlestick bar includes date/time, O/H/L/C, and volume with `tabular-nums` styling, rendered as a positioned overlay (no layout shift); ArrowRight from the focused `1D` button moves focus to `5D` and Enter fires `onRangeChange("5D")`; unmount (and a StrictMode double-mount cycle) calls `chart.remove()` once per created chart; token colors read via `getComputedStyle(document.documentElement)` are wrapped as `hsl(<triple>)` and up/down colors come from `--tw-success`/`--tw-destructive` (seed the jsdom root with token values in the test).
- [x] 5.5 Implement `gui/web/src/components/market-chart.jsx`: chart creation in a mount `useEffect` with a container ref, `autoSize: true`, `chart.remove()` in the effect cleanup (idempotent under StrictMode); no enter animation on first paint; mode-switching and `series`/`prevClose`/`showVolume` prop changes applied via effects that update the existing chart (recreate series on `mode` change; `setData` on `series` change); render exactly `series[0]` in area/candlestick modes and at most 6 series in indexed mode; indexed colors from `SERIES_COLORS` assigned by initial position and held in a symbol→color map so removals never repaint survivors; legend row for ≥2 rendered series (colored dot + text-token symbol text) and line-end direct labels for 2–4; crosshair tooltip (subscribe `crosshairMove`, render an absolutely positioned overlay div with date/time, OHLC or price — per-series in indexed mode — and volume, all `tabular-nums`); volume histogram on a dedicated price-scale id with `scaleMargins` confining it to the bottom of the pane; container `aria-label` per spec; range selector row rendered only when `onRangeChange` is provided — buttons with `aria-pressed` on the active range, ≥40×40px hit areas, `active:scale-[0.96]`, property-scoped transitions only, roving ArrowLeft/ArrowRight focus (reuse the index math pattern in `gui/web/src/features/market-state/shared.jsx:368`) and Enter/Space activation; theme tokens read at mount + a `MutationObserver` on `document.documentElement` (`class` and `data-theme` attributes) that re-reads tokens and calls `applyOptions` (GUI is light-only today — this is forward-compat per design D6); grid/axis colors from `--tw-border`/`--tw-muted-foreground`; `attributionLogo` left at its default (do not pass it). Named export `MarketChart`; no default export; no fetching; any tick animation CSS-only so the global reduced-motion rule (`gui/web/src/styles.css:269`) governs it.
- [x] 5.6 Lazy-load boundary: export the component for consumers behind `React.lazy(() => import("./market-chart.jsx"))` guidance — add a short comment block at the top of the file stating consumers must dynamic-import it (design D9); do not eagerly import it from any existing page in this change.

## 6. useInstrumentHistory hook (TDD)

- [x] 6.1 Failing tests in `tests/unit/gui-web/use-instrument-history.test.ts` (mock `globalThis.fetch`): initial call fetches `/api/instruments/history?symbol=AAPL&range=1D` and exposes `{ snapshot, loading, error }`; `range` change to `1Y` refetches with `loading: true` until resolve; out-of-order resolution (1D resolves after 1Y was issued) leaves the `1Y` snapshot in place; fetch failure sets `error` and clears `loading`; `stale`/`dataAsOf` pass through from the response body.
- [x] 6.2 Implement `gui/web/src/hooks/useInstrumentHistory.jsx` (sibling to `useMarketState.jsx`, same conventions): request-sequence counter or `AbortController` to discard superseded responses; re-fetch effect keyed on `[symbol, range]`.

## 7. Attribution surface

- [x] 7.1 Add the two-line NOTICE attribution ("TradingView Lightweight Charts™" / "Copyright (c) 2025 TradingView, Inc. https://www.tradingview.com/") to the GUI Diagnostics page (or an about section if one exists — implementer's choice per design Open Questions), with a unit assertion in the corresponding render test that the text is present.

## 8. Verification

- [x] 8.1 `npm test` green; `npx tsc --noEmit` green; `npx biome ci .` green; React Doctor clean on changed GUI files (autoreview runs it — no error-level diagnostics).
- [x] 8.2 Live check: `npm run gui`, hit `http://127.0.0.1:14567/api/instruments/history?symbol=AAPL&range=1D` from the GUI origin and confirm distinct intraday `time` values; mount `MarketChart` in a scratch route or Vitest browser harness only if needed — production mounts belong to `symbol-page`/`chat-answer-charts`.
- [ ] 8.3 GUI screenshots at 1440x960 and 390x844 of a rendered chart (area + candlestick, range selector visible; from the scratch mount or a consumer branch) for the PR description.
- [x] 8.4 CHANGELOG `[Unreleased]` → Added: history endpoint + `MarketChart` component entry (one atomic entry; use the changelog-automation skill).
- [x] 8.5 `graphify update .`
- [x] 8.6 `npx openspec validate market-chart --strict` passes (command verified available in this repo).
