# Tasks

Follow AGENTS.md (TDD, fixtures, no live API calls in unit tests, CHANGELOG, `graphify update .`). PRECONDITION: the `market-chart` change must be implemented first — `<MarketChart>` is a hard dependency. Its authored contract (market-chart design D6 / spec "MarketChart renders provided series data and never fetches") is what these tasks consume: `series: [{symbol, bars: [{time, open, high, low, close, volume}], indexed?: number[]}]` with `time` in UTC epoch seconds, `mode: 'area' | 'candlestick' | 'indexed'`, and no range selector when `onRangeChange` is absent. Persisted tool `OHLCV` bars carry `timestamp` (market-chart's additive field), never `time` — the card wrappers map it. Before task 3, confirm the landed implementation matches that spec; if it deviates, adapt only the card wrappers. Before task 4, resolve the per-series color seam (design "Per-series color seam" risk: additive `series[].color` prop coordinated with market-chart, else MarketChart consumes the shared series-color module internally).

## 1. get_price_comparison tool (TDD)

- [x] 1.1 Add fixture `tests/fixtures/yahoo/MSFT-history.json` mirroring the shape of `tests/fixtures/yahoo/AAPL-history.json` (raw Yahoo history response for `getHistory`), with date coverage overlapping AAPL's so alignment tests have common dates plus at least one MSFT-only date. Do not modify `AAPL-history.json`.
- [x] 1.2 Write failing unit tests in `tests/unit/tools/price-comparison.test.ts` (mock `globalThis.fetch` with fixture JSON, following `tests/unit/tools/crypto-history.test.ts`/`stock-quote.test.ts` patterns): (a) two-symbol success — `details.series` length 2, bars date-aligned to the common dates, each `indexed[0] === 100`, `indexed[i]` equals `close[i]/close[0]*100`, `baseDate` = first common date; (b) params schema rejects 1 symbol and 7 symbols (`minItems: 2`, `maxItems: 6`) and rejects a range outside `HISTORY_RANGES`; (c) per-symbol fallback — Yahoo fetch fails for one symbol, Alpha Vantage fixture serves it, other symbol unaffected; (d) one-of-three unavailable from all providers → 2 aligned series + that symbol in `details.unavailableSymbols` + named in content text; (e) all-but-one unavailable → explicit unavailability text, `details.series` empty; (f) misaligned calendars — MSFT-only date dropped from every aligned series; (g) >30% intersection loss — a fixture pair whose alignment drops more than 30% of one series' fetched bars (7-day-week vs 5-day-week calendar, or a synthetic sparse series) → content text notes the reduced aligned window naming the affected symbol, and a below-threshold pair produces no such note; (h) content text has exactly one table row per surviving symbol (symbol, first aligned close, last aligned close, % change over the aligned window = indexed end − 100) and ends with an as-of line from `formatAsOfLine` using the latest aligned bar date; freshness stamp present in `details.freshness`.
- [x] 1.3 Export the per-symbol fetch from `src/tools/market/stock-history.ts`: extract lines 60-69 (the `withFallback([yahoo, alphavantage])` / `wrapProvider("yahoo", ...)` branch) into an exported `fetchHistoryWithFallback(symbol, range, interval): Promise<ProviderResult<OHLCV[]>>`, export `HISTORY_RANGES` and `HISTORY_INTERVALS`, and re-run existing `get_stock_history` tests unchanged. Do not change `get_stock_history` behavior.
- [x] 1.4 Implement `src/tools/market/price-comparison.ts`: Typebox params (`symbols: Type.Array(Type.String(...), {minItems: 2, maxItems: 6})`, `range` union from `HISTORY_RANGES`, optional `interval` union from `HISTORY_INTERVALS` defaulting `1d`), named export `priceComparisonTool`, tool name `get_price_comparison`. Per symbol call `fetchHistoryWithFallback`; collect `unavailableSymbols`; intersect dates across surviving series (dates present in ALL series); compute `indexed` over the aligned bars; build `details` per the spec's canonical shape (`{ range, interval, baseDate, series, unavailableSymbols, freshness }`) and the content text in spec order — table (one row per surviving symbol: symbol, first aligned close, last aligned close, % change over the aligned window), >30%-intersection-loss note when triggered, unavailable-symbols line, then the `buildFreshnessStamp`/`formatAsOfLine` as-of line (`src/infra/freshness.ts`; use the latest aligned bar date as `asOf`, per the `src/tools/market/stock-quote.ts:44-66` pattern). No analysis language in the output — numbers and dates only. All 1.2 tests green.
- [x] 1.5 Register the tool: import/export/add `priceComparisonTool` in `src/tools/index.ts` (alongside `stockHistoryTool`, :15/:48/:72) and add `"get_price_comparison"` to the `core_market` array in `src/routing/route-manifest.ts` `TOOL_BUNDLE_TOOLS`. Confirm `tests/unit/tools/tool-schema-guardrails.test.ts` passes with the new tool.

## 2. Event-pipeline round-trip assertion

- [x] 2.1 Add one assertion to `tests/unit/gui-server/chat-event-adapter.test.ts` (write it before relying on it in card work): a toolResult session entry whose details is a large multi-symbol OHLCV payload (2 series × ~100 bars) adapted via `sessionEntriesToChatEvents` and reduced by `gui/shared/event-reducer.ts` yields a `tool.completed` output whose `details` deep-equals the input payload. No production change expected — this pins the seam the cards depend on.

## 3. History card swap (TDD) — requires landed market-chart

- [x] 3.1 Write failing static-markup render tests in `tests/unit/gui-web/history-card-render.test.ts`, copying the pattern of `tests/unit/gui-web/stock-quote-card-render.test.ts`: (a) `HistoryCard` with an OHLCV `details` array renders a MarketChart-backed chart plus the existing header/stat tiles; (b) bars WITHOUT a `timestamp` field (pre-market-chart persisted shape, `{date,open,high,low,close,volume}` only) render without error, deriving `time` from `date`; (c) bars WITH a finite epoch-seconds `timestamp` map it to the MarketChart bar `time`; (d) a `get_crypto_history`-shaped message renders through the same card; (e) the chart container carries the bounded-height classes (`h-[180px] sm:h-[220px]`) and `overflow-hidden`, and no range-selector markup renders; (f) the card states its covered window as the first and last bar dates.
- [x] 3.2 Swap `HistoryCard` internals in `gui/web/src/features/renderers/cards/market.jsx` (:111-152): keep header and stat tiles, replace `<PriceChart bars={bars} change={change} />` (:140) with the shared MarketChart as a single series (`series={[{symbol, bars}]}`, area mode; map bar `timestamp` → `time` when finite, else parse `date` as UTC midnight). Bound the chart in a `h-[180px] sm:h-[220px]` `overflow-hidden` container (MarketChart `autoSize` tracks it); add a first→last bar-date window line to the header area; do not pass `onRangeChange`. Delete `PriceChart`, `HoverTooltip`, `niceFloor`, `niceCeil`, `buildTicks`, `pickTickIndices`, `formatTick`, `formatDateLong` (:154-372). Do not touch `CompareCard` (:374) or `cards/index.jsx` entries for history tools. Add NO range switching and NO fetching in the card.
- [x] 3.3 All 3.1 tests green; run the full gui-web unit suite to catch collateral use of the deleted helpers.

## 4. Comparison chart card (TDD) — requires landed market-chart

- [x] 4.1 Verify `gui/web/src/lib/series-colors.js` exists and exports `SERIES_COLORS` (landed by the market-chart change's task 5.2, which owns the `ALLOCATION_COLORS` extraction). If the module is missing, STOP — the `market-chart` change has not landed and it is a hard dependency; do not extract the palette in this change. Card colors use `SERIES_COLORS[index]` by `details.series` position.
- [x] 4.2 Write failing render tests in `tests/unit/gui-web/price-comparison-card-render.test.ts` (same static-markup pattern): (a) a `get_price_comparison` message with two indexed series renders a multi-series indexed MarketChart with a legend entry per symbol (colored dot + symbol text) and a subtitle reading `% change, indexed to 100 at {baseDate}`; (b) legend/end-label colors follow `series-colors.js` order by `details.series` position, unchanged when a requested symbol is in `unavailableSymbols`; (c) ≤4 series render direct end-of-line symbol labels; (d) non-empty `unavailableSymbols` renders a `WarningRow` naming the symbols; (e) the chart container carries `aria-label` `Indexed price comparison of {symbols} over {range}`, bounded-height classes, and no range-selector markup; (f) no currency symbol appears in the legend, subtitle, or end-of-line labels; legend/tooltip numerals use `tabular-nums`; (g) details read via `extractDetails(message)` (handles `details.value` wrapping); (h) an existing `compare_companies` message still renders `CompareCard`'s metrics table unchanged (regression pin).
- [x] 4.3 Implement `gui/web/src/features/renderers/cards/price-comparison.jsx`: props per `ToolResultCard.jsx:30-35`; compose `ToolCard`/`ToolHeader` + scope line (`details.range` + `formatAsOfLine`-style as-of from `details.freshness`) + indexing-basis subtitle + MarketChart (`mode="indexed"`, precomputed `indexed` passed through, `h-[180px] sm:h-[220px]` `overflow-hidden` container, no `onRangeChange`) + legend + end-of-line labels (≤4 series) + `WarningRow` for `unavailableSymbols`; crosshair tooltip shows date + per-series indexed value to one decimal + raw close in parens, `tabular-nums`; colors via `SERIES_COLORS[index]` positional assignment over `details.series` (MarketChart applies the same module internally to the indexed lines — see market-chart task 5.5); transitions, if any, name specific properties — never `transition: all`; no fetching. Register in `cards/index.jsx`: `["get_price_comparison", { category: "Comparison", Component: PriceComparisonCard }]`. Leave the `compare_companies` → `CompareCard` entry untouched.
- [x] 4.4 All 4.2 tests green.

## 5. Compare workflow prompt

- [x] 5.1 Add one generic numbered step to `buildCompareAssetsPrompt` (`src/prompts/workflow-prompts.ts:367`, Steps block) instructing: use `get_price_comparison` with the resolved symbols and a range fitting the resolved time horizon (default when none) so the answer includes an indexed price comparison. No ticker/sector/benchmark-specific wording (AGENTS.md prompt rules). Renumber/interpolate consistently with the existing conditional step numbering.
- [x] 5.2 Update expectations in `tests/unit/prompts/workflow-prompts.test.ts` and the prompt-output snapshots (`tests/unit/prompts/prompt-output-snapshots.test.ts` + `__snapshots__`), then run `npx vitest run tests/unit/prompts/prompt-debt-guard.test.ts` — must pass; keep any benchmark literals in manifests/tests only.

## 6. Verification

- [x] 6.1 `npm test`, `npx tsc --noEmit`, `npx biome ci .` all green; React Doctor clean on the changed GUI files (`market.jsx`, `index.jsx`, `price-comparison.jsx`).
- [ ] 6.2 Live GUI evidence: run `npm run gui`, drive one `get_stock_history` prompt and one compare prompt that triggers `get_price_comparison`; screenshot both cards at 1440x960 and 390x844, confirming chart heights (220px / 180px) and that the 390px transcript has no horizontal scroll; reload the session and confirm both cards render identically from persisted entries (spec: live/reload parity). Confirm the TUI (`npm start`) shows the comparison tool's plain-text table.
- [x] 6.3 Add CHANGELOG `[Unreleased]` entries (history card on shared MarketChart; new `get_price_comparison` tool + comparison chart card + compare-workflow wiring).
- [x] 6.4 `graphify update .`; validate the change with `npx openspec validate chat-answer-charts --strict` (if that command is unavailable in the installed openspec version, run `npx openspec status --change chat-answer-charts --json` and confirm completeness).
