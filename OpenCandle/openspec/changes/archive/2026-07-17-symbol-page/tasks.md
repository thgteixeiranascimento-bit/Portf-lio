# Tasks

Follow AGENTS.md (TDD, CHANGELOG, `graphify update .`). HARD dependency: implement only after the `market-chart` change has landed (`<MarketChart>`, `useInstrumentHistory`, `/api/instruments/history`) — verify those exist in gui/web before starting; their contracts are fixed by the `market-chart` spec (stable `MarketChart` prop API; `useInstrumentHistory(symbol, range)` → `{snapshot, loading, error}` in gui/web/src/hooks/). Reuse existing primitives (shared.jsx, view models, memo patterns); do not fork new ones. Visual and interaction rules come from design.md "UI/UX guidance" plus DESIGN.md at the repo root. All new GUI files are React `.jsx` under gui/web/src, server files are TypeScript under gui/server with `.js` extensions on relative imports.

## 1. Route resolution (TDD)

- [x] 1.1 Failing unit test for `tickerFromPath(pathname)` (new file colocated with its export; test under tests/unit/gui-web/): `/symbol/AAPL` → `AAPL`; `/symbol/aapl` → `AAPL`; `/symbol/%5EGSPC` → `^GSPC`; `/symbol/BRK-B` → `BRK-B`; `/symbol/BTC-USD` → `BTC-USD`; no match for `/symbol/`, `/symbol/A/B`, `/watchlists`, `/`, and charset-rejected segments like `/symbol/%3Cscript%3E`. Implement as an exported pure helper mirroring `domainFromPath` (gui/web/src/App.jsx:499-505).
- [x] 1.2 Add the `/symbol/$ticker` route to gui/web/src/router.jsx with `validateSearch: validateGuiSearch`, matching the existing entries (router.jsx:10-58).
- [x] 1.3 Add the AppShell branch in gui/web/src/App.jsx: compute `tickerFromPath(pathname)` and render `<SymbolPage>` after the `/diagnostics` check and BEFORE the `marketDomain` branch (App.jsx:394-448). Pass `startChatRun={startRoutedChatRun}`, `navigate`, `invokeTool={invokeToolForVisibleSession}`, `role={gui.role}`, `setToast={gui.setToast}`, plus the sidebar props the other pages receive. Add a precedence test (diagnostics → symbol → market-state → chat) using `renderToStaticMarkup` or the helper-level equivalent.

## 2. Server: overview endpoint + marketCap fix (TDD)

- [x] 2.1 Failing unit test for `getInstrumentOverviewSnapshot(symbol)` in gui/server/market-state-api.ts with `globalThis.fetch`/provider mocked per repo convention (fixture JSON in tests/fixtures/yahoo-finance/ if a new response shape is needed): ok shape `{symbol, status: "ok", name, description, exchange, sector, industry, marketCap, pe, forwardPe, eps, dividendYield, beta, avgVolume, profitMargin, revenueGrowth, week52High, week52Low, stale}`; empty/unknown symbol → `{symbol, status: "unavailable", reason}`; wraps `getYahooCompanyOverview` (src/providers/yahoo-finance.ts:125) via `wrapProvider("yahoo", …)` like `getInstrumentQuoteSnapshot` does for `getQuote` (market-state-api.ts:559).
- [x] 2.2 Failing unit test for the per-symbol SWR memo (model on `createSavedSymbolsMemo`, market-state-api.ts:122, and `QuoteSnapshotStore`, gui/server/quote-snapshot-store.ts:3): two concurrent calls for one symbol → one upstream call; second call within the ~5 min TTL → served from memo, no upstream call; after TTL → revalidates while serving the previous value; distinct symbols do not share entries. Implement the memo in front of the provider call.
- [x] 2.3 Failing route test for `GET /api/instruments/overview?symbol=` in gui/server/http-routes.ts next to the existing `/api/instruments/quote` handler (http-routes.ts:254): guarded by `allowTrustedGuiRequest` (untrusted request rejected without a provider call — copy an existing guard test), missing `symbol` param → unavailable response, ok path returns the memoized snapshot as JSON. No session creation anywhere on this path.
- [x] 2.4 Failing unit test that `getInstrumentQuoteSnapshot` (market-state-api.ts:482) returns `marketCap` from the underlying `getQuote` result; implement by adding `marketCap` to the returned object and its return type (additive). Then grep tests and gui/web consumers of `/api/instruments/quote` / `getInstrumentQuoteSnapshot` for exact-shape assertions and confirm none break (`grep -rn "getInstrumentQuoteSnapshot\|instruments/quote" tests gui`).

## 3. SymbolPage feature (TDD, static-markup render tests per tests/unit/gui-web/stock-quote-card-render.test.ts)

- [x] 3.0 Move `InspectorSection` from gui/web/src/features/market-state/WatchlistPage.jsx:543 to shared.jsx as a named export; update WatchlistPage to import it; existing WatchlistPage render tests stay green (no visual change).
- [x] 3.1 Failing render test for `useSymbolData` section derivation (pure derivation functions preferred so they test without hooks): given seeded market-state (portfolio lots, alerts, watchlist items, quote snapshot) and a ticker, position rows come from `buildHoldingRows` filtered on symbol and alert rows from the alert-view-model filtered on the symbol's `instrumentId` — mirror `SymbolInspector` (gui/web/src/features/market-state/WatchlistPage.jsx:421-437). Implement `useSymbolData(ticker)` in gui/web/src/features/symbol/ composing `useMarketState()` + fetches of `/api/instruments/quote` and `/api/instruments/overview` + `useInstrumentHistory(symbol, range)` (from market-chart); SymbolPage owns the current range state and passes `range`/`onRangeChange` to `<MarketChart>` (the selector itself belongs to MarketChart).
- [x] 3.2 Failing render tests for `SymbolHeader`: an `h1` with company name + symbol (`text-balance`); the price as the dominant element in `tabular-nums`; a direction-colored change chip containing an arrow icon plus the signed change value (direction never color-only — assert the arrow/sign render for both directions); currency; market-state badge; extended-hours line via the unmodified `ExtendedHoursQuote` (gui/web/src/features/market-state/shared.jsx:184) when `marketState` is PRE/POST; no extended line during REGULAR. Implement using shared.jsx primitives (`money`, `SignedPercent`, `Panel`) — no ad-hoc `toFixed`.
- [x] 3.3 Failing render tests for `KeyStats`: a flat divided stat grid with `dl`/`dt`/`dd` semantics (muted `dt` labels, right-aligned `tabular-nums` `dd` values — the entity-popover/mobile-portfolio-summary pattern, PortfolioPage.jsx:424-449); renders overview stats; omits stats with `null` or provider-zero placeholders (`pe: null`, `marketCap: 0` → stat absent, never "$0.00"); entire grid absent when overview is unavailable. Implement.
- [x] 3.4 Failing render tests for PositionCard / AlertsCard / WatchlistMembership: populated states from 3.1 fixtures (P&L numerals in `tabular-nums` via shared helpers); empty states when the ticker has no saved context; membership add-to-watchlist affordance present for writers.
- [x] 3.5 Failing render tests for `AnalyzePanel`: chips are symbol-templated `[label, prompt]` pairs per the `DEFAULT_PROMPTS` pattern (gui/web/src/components/chat/home-prompts.js:3) — include a quote chip, an options-chain chip, and `["Deep research: ${T} (multi-analyst, takes a few minutes)", "/analyze ${T}"]`; clicking a chip calls the passed `startChatRun` with the templated prompt (writer); when `role !== "writer"`, all chips and mutation controls are disabled but keep visible labels with neutral availability copy. Chips/buttons: ≥40x40px hit area, `active:scale-[0.96]` press feedback, property-scoped transitions (no `transition: all`). Implement; wire add-to-watchlist / create-alert through `invokeTool("manage_watchlist", …)` / `invokeTool("manage_alerts", …)` with acknowledged-result handling per existing market-state mutation conventions.
- [x] 3.6 Failing render tests for the non-equity branch and not-found state: `BTC-USD` / `^GSPC` (asset-type rules per src/market-state/resolve.ts:99-100, plus overview-unavailable fallback) render header + chart only, no KeyStats; both endpoints unavailable → not-found state naming the symbol, app shell still usable. Implement the branching in SymbolPage.jsx.
- [x] 3.7 Assemble `SymbolPage.jsx` (header, MarketChart, KeyStats, PositionCard, AlertsCard, membership, AnalyzePanel — fixed order) with a full-page render test over a seeded ok-path fixture asserting: a single `main` landmark with one `h1`, each section a labeled group, sections in header → chart → stats → position → alerts → analyze order, and no document-level horizontal overflow container (wide content wrapped in its own `overflow-x` scroller).
- [x] 3.8 Loading/refresh/staleness states (failing tests first): initial mount renders `Skeleton` (gui/web/src/components/ui/skeleton.jsx) blocks for header/chart/stats sized to final section heights, replaced in place (no enter animation classes on first paint); price-change flash reuses `useQuoteChangeFlash` (shared.jsx:267) + `quoteFlashClass` (shared.jsx:286) on the price row; staleness badge comes from `degradedQuoteBadge` (gui/web/src/features/market-state/format.js:25) only — assert no badge for fresh quotes and no new indicator component. Implement.

## 4. Entry-point wiring (TDD per surface)

- [x] 4.1 WatchlistPage QuoteBoard: symbol cell renders a link to `/symbol/$ticker` (URL-encoded; test `^GSPC` → `/symbol/%5EGSPC`) while the inline inspector affordance remains. Render test first, then implement.
- [x] 4.2 PortfolioPage rows: same symbol-cell link treatment; render test first.
- [x] 4.3 entity-popover.jsx: add "Open $X page" beside "Ask about $X" (entity-popover.jsx:164); extend tests/unit/gui-web/entity-popover.test.ts with the new action and its navigation target first.
- [x] 4.4 SymbolInspector: add an "Open full page" link; render test first.
- [x] 4.5 Instrument search (`SymbolSearchInput`, gui/web/src/features/market-state/MarketStatePage.jsx:936, backed by gui/web/src/features/instruments/use-instrument-search.js): selecting a candidate can navigate to `/symbol/$ticker`; test the navigation callback wiring first. Every link added in 4.1-4.5 is a real anchor/button with the ui primitives' focus-visible ring.

## 5. Verification

- [x] 5.1 `npm test`, `npx tsc --noEmit`, `npx biome ci .` green; React Doctor clean on changed GUI files (via `npm run review:pr` tooling or direct run).
- [ ] 5.2 Live check: `npm run gui`, open `/symbol/AAPL` (equity, full page), `/symbol/BTC-USD` (no KeyStats), `/symbol/ZZZZZZ` (not-found) as writer; open the same page in a second follower window and confirm disabled-but-labeled affordances with neutral copy and no session creation (session list unchanged). Confirm a writer analyze chip navigates to the new session. At 390px width, confirm single-column section order and no document-level horizontal scrolling.
- [ ] 5.3 Screenshots at 1440x960 and 390x844 for the PR: equity page, non-equity page, follower read-only state.
- [x] 5.4 CHANGELOG `[Unreleased]` entries (symbol page + entry points; overview endpoint; marketCap fix) per the changelog-automation skill.
- [x] 5.5 `graphify update .`; `npx openspec validate symbol-page --strict` passes.
