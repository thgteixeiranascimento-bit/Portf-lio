// A fixed, locale independent word so the confirmation reads the same for
// everyone and cannot be satisfied by a translated phrase. Lives apart from
// the dialog component so that module exports components only.
export const TYPED_CONFIRM_WORD = "DELETE";
