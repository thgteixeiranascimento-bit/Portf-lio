# Tasks

Follow AGENTS.md (TDD mandatory — failing test first for every code task; `.js` extensions on all relative imports; shared `cache`/`rateLimiter` from `src/infra/`; unit tests mock `globalThis.fetch` with fixtures, never live calls). Design decisions D1-D7 and the "User-facing surface" section in `design.md` govern how; the spec in `specs/lse-data-provider/spec.md` governs what. All paths below are repo-relative from `/Users/kahtaf/Documents/workspace/opencandle`.

## 1. Milestone 0 — ToS review (STOP gate)

- [x] 1.1 HUMAN TASK (cannot be done by a coding agent — request it and wait): open https://londonstrategicedge.com in a real browser (pages are JS-rendered; curl returns nothing useful), locate the Terms of Service / fair-use terms, and read the clauses on (a) caching/local persistence of API data, (b) redistribution, (c) commercial use. Record the verdict verbatim, with date and URL, in the PR description.
- [x] 1.2 DECISION POINT — STOP GATE: if caching or local persistence of LSE data is disallowed, STOP all remaining tasks, report the finding, and close this change without merging. Only proceed to section 2 after an explicit "caching permitted" verdict is recorded.

**ToS verdict (recorded 2026-07-16, https://londonstrategicedge.com/terms/, last updated 19 January 2026; signed off by the repo owner):** caching permitted — no clause prohibits local, per-user caching or persistence; the Acceptable Use restriction covers "Scrape, harvest, or extract data without permission" (a keyed API is the permission mechanism). Redistribution/resale and commercial exploitation of LSE data are prohibited ("Redistribute, resell, or commercially exploit our data or services") — compatible with OpenCandle's bring-your-own-key model: each user fetches with their own free key for personal analysis and OpenCandle never ships or re-serves LSE data. Data is provided "as is" without warranty. Gate cleared; merge unblocked.

## 2. Fixtures

- [x] 2.1 Create `tests/fixtures/lse/candles-AAPL-1d.json`: a JSON array of candle rows in the documented shape, e.g. `[{"ts":"2026-07-02 00:00:00.000000","symbol":"AAPL","open":296.02,"high":309.28,"low":293.7,"close":308.48,"volume":25934709}, ...]` — at least 3 rows with ascending timestamps.
- [x] 2.2 FIXTURE-FIRST (hard prerequisite for section 6 — the exact `/ref/financial_reports` row schema was not captured from the docs, so the mapper must never be written against guessed field names): obtain a throwaway LSE key, fetch one real response each for `symbol=AAPL&report_type=income&period=FY`, `report_type=balance`, and `report_type=cashflow`, and save the verbatim JSON as `tests/fixtures/lse/financial-reports-AAPL-income.json`, `financial-reports-AAPL-balance.json`, and `financial-reports-AAPL-cashflow.json`. Write the 6.2 mapper against these saved fixtures only. Do not start section 6 until this task is done.
- [x] 2.3 Create `tests/fixtures/lse/error-429-allowance.json`: `{"detail": "Monthly data allowance exceeded"}` (update the string to the live wording when first observed; the parser must treat any 429 `detail` mentioning the monthly allowance as terminal and all other 429s as rate-limit retryable — see design D2).

## 3. Infra wiring (config, rate bucket, cache domains, registry)

- [x] 3.1 TDD: add failing unit tests (extend the existing config test file for `src/config.ts`) asserting: `getConfig().lseApiKey` resolves from `process.env.LSE_API_KEY`; falls back to file config `providers.lse.apiKey`; env wins over file. Then implement: add `lseApiKey?: string;` to the `Config` interface (src/config.ts:21-46), add `lse?: { apiKey?: string };` under `OpenCandleFileConfig.providers` (config.ts:48-65), and add `lseApiKey: process.env.LSE_API_KEY ?? fileConfig.providers?.lse?.apiKey,` in `resolveConfig` (config.ts:190-198, next to the alphaVantage line).
- [x] 3.2 Add `rateLimiter.configure("lse", 100, 1.66); // 100 req/min (free tier)` to the bucket block at the bottom of `src/infra/rate-limiter.ts` (~lines 57-77, after the sec_edgar line).
- [x] 3.3 Add cache domains to `src/infra/cache.ts`: in `TTL` (lines 101-113) add `CANDLES: 3_600_000, // 1 hour` and `FINANCIAL_REPORTS: 86_400_000, // 24 hours`; in `STALE_LIMIT` (lines 116-127) add `CANDLES: 24 * 3_600_000, // 24 hours` and `FINANCIAL_REPORTS: 7 * 86_400_000, // 7 days`.
- [x] 3.4 TDD: add failing unit tests for registry lookup (`getProvider("lse")` returns the descriptor; `hasCredential("lse")` reflects `getConfig().lseApiKey`). Then implement in `src/onboarding/providers.ts`: (1) add `"lse"` to the `ApiKeyProviderId` union (line 13); (2) add a descriptor object to the `PROVIDERS` array with `id: "lse"`, `kind: "api-key"`, `displayName: "London Strategic Edge"` (never "LSE" — reads as the London Stock Exchange; see design "User-facing surface"), `envVar: "LSE_API_KEY"`, `configPath: ["providers", "lse", "apiKey"]`, `signupUrl: "https://londonstrategicedge.com/databank"`, `freeTier: true`, `tier: "soft"`, `category: "market"`, `aliases: ["lse", "london strategic edge", "londonstrategicedge"]`, `unlocks` naming financial statements + deep intraday history, a `fallbackDescription` naming Yahoo/Alpha Vantage, and `snoozeDurationDays`/`instructionsHint` values matching the other soft api-key descriptors; (3) add `lse: "lseApiKey",` to `CONFIG_FIELD_BY_ID` (providers.ts:359-365). The exhaustiveness checks at providers.ts:270-275 and the `Record<ApiKeyProviderId, ...>` type will fail compilation until all three land — that is the checklist.
- [x] 3.5 Verify doctor visibility needs no code: run the provider-status unit tests; `probeAllProviderStatuses` (src/onboarding/provider-status.ts:150-159) iterates `listAllProviders()`, so LSE appears automatically. Add assertions to the existing provider-status test that an unconfigured `"lse"` probes as skipped/not-configured (soft tier), does not degrade overall doctor health, and renders as "London Strategic Edge" (via `formatProviderStatus`, provider-status.ts:161-170).

## 4. Byte budget (net-new subsystem)

- [x] 4.1 TDD: create failing unit tests at `tests/unit/infra/lse-byte-budget.test.ts` (match the repo's existing unit-test layout; use a temp dir as OpenCandle home) covering: `recordBytes` accumulates across calls and persists as JSON `{ month: "YYYY-MM", bytesUsed: number }`; a stored month different from the current month reads as zero (lazy reset); `isOverSoftThreshold()` is false below and true at/above `42_949_672_960` bytes (80% of 50 GiB = 53_687_091_200); `markExhausted()` jumps `bytesUsed` to the full cap; a corrupt/missing file reads as zero without throwing (fail-open, spec scenario "Corrupt budget file fails open").
- [x] 4.2 Implement `src/infra/lse-byte-budget.ts`: module-level singleton export (repo infra convention), persisting to the JSON file `lse-byte-budget.json` under the OpenCandle home resolved via `src/infra/opencandle-paths.ts` — follow the read/write precedent of `src/onboarding/state.ts`. Exports: `recordBytes(n: number): void`, `isOverSoftThreshold(): boolean`, `markExhausted(): void`. Use `.js` import extensions. Do NOT put the gate inside the provider fetch — gating happens at tool chain assembly (design D4).
- [x] 4.3 TDD: add a failing doctor test (extend the existing doctor report tests) for the soft-threshold advisory, then implement in the doctor providers section (`src/doctor/report.ts`, `buildProviderChecks`): when `getConfig().lseApiKey` is set AND `isOverSoftThreshold()` is true, emit exactly one `warn`-status check whose message is `London Strategic Edge monthly data allowance is <percent>% used; answers use Yahoo/Alpha Vantage until the month resets.` (`<percent>` = `bytesUsed / 53_687_091_200 * 100`, rounded to a whole number), rendered through the existing one-line check + `Fix:` remediation path (src/doctor/render.ts:26); assert no such check appears when the key is missing or usage is under threshold (spec scenario "Doctor surfaces a soft-threshold advisory").

## 5. Provider module src/providers/lse.ts

- [x] 5.1 TDD: create failing unit tests at `tests/unit/providers/lse.test.ts` (mock `globalThis.fetch`, load fixtures from `tests/fixtures/lse/`) covering every spec scenario of "LSE provider module": request URL is `https://api.londonstrategicedge.com/vault/candles?...` with `x-api-key` header; second call within TTL hits cache (assert fetch called once); 401/403 with `{"detail"}` throws `ProviderCredentialError("lse", "stale", status)` even when stale cache exists; no configured key throws `ProviderCredentialError("lse", "missing")` with zero fetch calls; 5xx after retries returns stale cache when present; 404 error exposes status + detail; every response's `X-Data-Bytes` header increments the byte budget (spy on the budget module); a 429 matching the allowance fixture calls `markExhausted()` and does not retry; a plain rate-limit 429 retries per Retry-After (capped 5s).
- [x] 5.2 Implement `src/providers/lse.ts`: internal `lseGet<T>(path: string, params: Record<string, string>): Promise<T>` using `globalThis.fetch` directly (NOT `src/infra/http-client.ts` — it hides response headers; do not modify it). `lseGet` must: read the key via `getConfig().lseApiKey` and throw `ProviderCredentialError("lse", "missing")` if absent; set the `x-api-key` header; on non-OK parse the JSON `{"detail"}` body; call `throwIfLseAuthError` (copy of the pattern at src/providers/alpha-vantage.ts:19-23, provider id `"lse"`) for 401/403; classify 429 via detail text (allowance → `markExhausted()` + throw immediately; otherwise retryable); retry up to 2 times on 5xx/retryable-429 honoring `Retry-After` capped at 5000ms (mirroring src/infra/http-client.ts norms); read `X-Data-Bytes` from every response and call `recordBytes(...)`. Public exports (verb-prefixed, typed returns): `getLseCandles(symbol: string, timeframe: LseTimeframe, opts?: { start?: string; end?: string; order?: "asc" | "desc"; limit?: number })` over `GET /candles`, and `getLseFinancialReports(symbol: string, reportType: "income" | "balance" | "cashflow", opts?: { period?: "FY" | "Q1" | "Q2" | "Q3" | "Q4"; start?: string; end?: string; order?: "asc" | "desc"; limit?: number })` over `GET /ref/financial_reports`. Both use the cache idiom: `cache.get` → `rateLimiter.acquire("lse")` → `lseGet` → `cache.set(key, val, TTL.CANDLES | TTL.FINANCIAL_REPORTS)`; catch → `throwIfLseAuthError` → `cache.getStale(key, STALE_LIMIT.CANDLES | STALE_LIMIT.FINANCIAL_REPORTS)` → rethrow if no stale value. Raw API row types may use `any`; public return types must be strictly typed interfaces.
- [x] 5.3 Re-export the public functions and types from `src/providers/index.ts` (match how alpha-vantage exports are re-exported there).

## 6. Tool integration: get_financials (LSE → Alpha Vantage)

- [x] 6.1 PREREQUISITE: task 2.2's real-response fixtures exist. TDD: add failing unit tests to the existing `get_financials` test file (find it under `tests/` next to the other fundamentals tool tests; mock fetch with the 2.2 LSE fixtures + AV fixtures) covering the three spec scenarios: LSE success → mapped `FinancialStatement[]`, zero AV requests, source labeled London Strategic Edge; LSE failure (each of: thrown error, missing key, `isOverSoftThreshold() === true`) → AV serves and is labeled; both fail → existing warning naming both reasons.
- [x] 6.2 Implement an exported mapper in `src/providers/lse.ts` that fetches the three `report_type`s (`income`, `balance`, `cashflow`) via `getLseFinancialReports` and assembles the existing `FinancialStatement[]` shape that `src/tools/fundamentals/financials.ts` consumes today (exact target fields: `FinancialStatement` at src/types/fundamentals.ts:32-47; the AV reference mapping is `getFinancials` at src/providers/alpha-vantage.ts:129-194). Write the field mapping against the 2.2 fixtures saved from real API responses — never against guessed field names. Missing line items map to the same absent/undefined convention the AV mapping uses — never fabricate numbers.
- [x] 6.3 Rework `src/tools/fundamentals/financials.ts` (current AV-only call at lines 23-28) to build a `withFallback` chain (src/providers/with-fallback.ts): entry 1 = `{ provider: "lse", fn: ... }` included only when an LSE key is configured AND `!isOverSoftThreshold()`; entry 2 = the existing Alpha Vantage path unchanged (keep `withCredentialCheck("alpha_vantage", ...)` semantics working for the AV leg). Preserve the existing degraded-warning output when the whole chain fails.

## 7. Tool integration: compute_dcf (LSE → Alpha Vantage → Yahoo)

- [x] 7.1 TDD: add failing unit tests to the existing `compute_dcf` test file covering: LSE fresh statements feed the DCF with no AV/Yahoo statement calls; LSE unavailable → existing AV → Yahoo behavior byte-for-byte; stale-only data from all sources → the existing explicit refusal message (dcf must never compute from stale statements — spec scenario "Stale LSE data is refused").
- [x] 7.2 Implement in `src/tools/fundamentals/dcf.ts`: prepend the LSE statements source (same budget + key gate as 6.3) ahead of the existing Alpha Vantage attempt, keeping the Yahoo fallback at dcf.ts:272 and all existing stale-refusal and unavailable-message wording intact.

## 8. Tool integration: get_stock_history (intraday fallback + deep range + timeframe mapping)

- [x] 8.1 TDD: add failing unit tests for a pure mapping function `toLseTimeframe(interval: string): LseTimeframe | undefined`, exported from `src/providers/lse.ts` — complete table per design D5: `1m→1m`, `5m→5m`, `15m→15m`, `1h→1h`, `1d→1d`, `1wk→1w`, `1mo→1mo`, anything else → `undefined`. Keep it pure (no config/budget reads).
- [x] 8.2 TDD: add failing unit tests to the existing `get_stock_history` test file covering the spec scenarios: intraday (5m) Yahoo failure + LSE key configured → LSE candles returned mapped to `OHLCV` (src/types/market.ts:26-33) with the design-D5 timestamp semantics — `date` = first 10 characters of the naive `ts` string; if the optional epoch-seconds `timestamp` field from the parallel market-chart change exists on `OHLCV`, it is populated by parsing `ts` as UTC — source labeled London Strategic Edge; `1wk` request reaching LSE sends `timeframe=1w`; no LSE key + intraday Yahoo failure → exact current message "No alternate source for {interval} data"; daily chain order is Yahoo → Alpha Vantage → LSE; budget over threshold removes the LSE entry.
- [x] 8.3 Implement in `src/tools/market/stock-history.ts` (current chain logic at lines 60-69): daily+ branch appends `{ provider: "lse", fn: ... }` after the Alpha Vantage entry; intraday branch becomes a `withFallback` of Yahoo then LSE (replacing the bare `wrapProvider("yahoo", ...)`) when `toLseTimeframe(interval)` is defined, an LSE key is configured, and `!isOverSoftThreshold()` — otherwise keep the current single-provider path and its "No alternate source" message. Pass the requested range through as ISO `start`/`end` so ranges beyond Yahoo's caps (1m ≈ 7 days, hourly ≈ 730 days) are served by LSE back to 2003.

## 9. Live e2e / drift canary

- [x] 9.1 Add an LSE check to the live provider e2e suite (the suite behind `npm run test:e2e:providers`, used by the nightly drift canary): with `LSE_API_KEY` present, call `getLseCandles("AAPL", "1d", { limit: 5 })` live and assert the row shape (`ts`, `open`, `high`, `low`, `close`, `volume` present and numeric where expected); without a key, record the check as an environment-limitation skip following the suite's existing skip semantics — never a failure.
- [ ] 9.2 While a live key is available, capture one real allowance-429 `detail` wording if observable and update the 2.3 fixture to the live wording (the `/ref/financial_reports` fixtures were already captured live in task 2.2; re-verify they still match current responses and adjust the mapper tests if the vendor drifted).
- [x] 9.3 Verify the design-D5 UTC assumption: confirm from `GET /meta`, the vendor docs, or by comparing a known regular-session intraday candle's `ts` against the exchange session time, whether LSE `ts` values are UTC. Record the finding in the PR description; if they are exchange-local (US/Eastern) instead, fix the `timestamp` parse in the 8.x mapping before release.

## 10. Verification

- [x] 10.1 `npm test` green.
- [x] 10.2 `npx tsc --noEmit` green (this also proves the registry exhaustiveness checks).
- [x] 10.3 `npx biome ci .` green.
- [x] 10.4 Add a CHANGELOG.md `[Unreleased]` → `### Added` entry describing the LSE provider, the byte-budget gating, and the three tool fallback-chain integrations (use the changelog-automation skill conventions).
- [x] 10.5 `graphify update .` to refresh the knowledge graph.
- [x] 10.6 `npx openspec validate lse-data-provider --strict` passes (the `validate` command exists — verified via `npx openspec --help` on 2026-07-16).
